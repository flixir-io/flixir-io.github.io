{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"docs/","title":"Base templates","text":"<p>Every Flixir workflow is built from two kinds of blocks: triggers, which start a run, and steps, which do the work. These are the built-in templates that ship with the <code>core</code> and <code>ai</code> integrations \u2014 available in every workflow, no setup required.</p>"},{"location":"docs/#guides","title":"Guides","text":"<p>How parameter fields work under the hood.</p> <ul> <li>Expressions \u2014 Reference and paths, JSONPath, and built-in functions for expression-aware fields.</li> <li>Storage \u2014 Store and query your own structured data inside workflows.</li> </ul>"},{"location":"docs/#triggers","title":"Triggers","text":"<p>A workflow starts with exactly one trigger. It defines the event that kicks off a run and the data available to the rest of the workflow.</p> <ul> <li>Manual Trigger \u2014 Runs a workflow on demand, straight from the Flixir editor. No external event required.</li> <li>Webhook \u2014 Fires a workflow whenever Flixir receives an HTTP request on the workflow's unique webhook URL.</li> <li>Scheduled \u2014 Runs a workflow on a recurring schedule using a standard cron expression, with ready-made presets.</li> <li>Shopify \u2014 Fires a workflow from Shopify store events \u2014 new products, updates, inventory changes, and more.</li> <li>Storage Changed \u2014 Fires a workflow whenever an item in a Storage is created, updated, or deleted.</li> </ul>"},{"location":"docs/#steps","title":"Steps","text":"<p>Steps are chained after a trigger to process data, call external services, branch logic, or run custom code.</p> <ul> <li>Debug \u2014 Outputs the current state or specified variables to the run log, for inspecting a workflow mid-flight.</li> <li>HTTP Request \u2014 Makes an HTTP request to any URL with a configurable method, query params, headers, and body.</li> <li>Lua script \u2014 Executes a custom Lua script with access to workflow variables, for logic no built-in step covers.</li> <li>Condition \u2014 Evaluates a boolean expression and branches the workflow down a true or false path.</li> <li>Loop \u2014 Iterates over a list, running the downstream branch once per item.</li> <li>Sleep \u2014 Pauses the workflow for a set amount of time before continuing.</li> <li>Shopify GraphQL \u2014 Executes any Shopify Admin GraphQL query or mutation directly against the connected store.</li> <li>Gemini \u2014 Generates text with Google Gemini \u2014 summarize, classify, or draft content mid-automation.</li> <li>Storage: Write \u2014 Inserts a new item into a Storage.</li> <li>Storage: Read \u2014 Looks up items in a Storage, with an optional single-field filter.</li> <li>Storage: Update \u2014 Merges new values into an existing Storage item, by id.</li> <li>Storage: Delete \u2014 Permanently removes an item from a Storage, by id.</li> </ul>"},{"location":"docs/expressions/","title":"Expressions","text":"<p>used in fields marked expression-aware</p> <p>Expressions let a field pull in live data instead of a fixed value \u2014 the trigger payload, a previous step's output, a secret, or the result of a function. They show up in fields like <code>expression</code> (Condition), <code>prompt</code> (Gemini), <code>list</code> (Loop), request bodies, and Shopify GraphQL queries and variables.</p>"},{"location":"docs/expressions/#literals","title":"Literals","text":"<p>The basic building blocks an expression can be made of:</p> Reference Example Description <code>string</code> <code>\"Hello, world\"</code> Double-quoted text. Use two double quotes with nothing between them for an empty string. <code>number</code> <code>42</code> Integers only \u2014 decimals aren't supported yet. <code>boolean</code> <code>true / false</code> Boolean literals, unquoted."},{"location":"docs/expressions/#references","title":"References","text":"<p>Dotted paths that resolve to a value at runtime:</p> Reference Example Description <code>trigger</code> <code>trigger</code> The entire trigger payload, unresolved. <code>trigger.&lt;path&gt;</code> <code>trigger.payload.title</code> A specific field inside the trigger payload. <code>steps.&lt;name&gt;</code> <code>steps.debug_a</code> The entire output of an upstream step, addressed by its machine name. <code>tenant.&lt;path&gt;</code> <code>tenant.id / tenant.name</code> The current tenant's ID or name. <code>env.&lt;path&gt;</code> <code>env.API_KEY</code> An environment-level value. <code>workflow.&lt;path&gt;</code> <code>workflow.id / workflow.name</code> The ID or name of the running workflow. <code>loop.item / loop.index</code> <code>loop.item</code> The current item and zero-based index \u2014 only inside a branch downstream of a Loop step. <code>secrets.&lt;KEY&gt;</code> <code>secrets.SHOPIFY_API_KEY</code> A workspace secret. Key must be UPPER_SNAKE_CASE. Never shown in logs. <code>vars.&lt;KEY&gt;</code> <code>vars.STORE_NAME</code> A workspace variable. Key must be UPPER_SNAKE_CASE. Logged as-is. <p>Note</p> <p><code>trigger</code>, <code>tenant</code>, <code>env</code>, <code>workflow</code>, and <code>loop</code> are reserved namespaces \u2014 typing one of these names by itself (no dot) resolves to its entire payload, where applicable.</p> <p>Note</p> <p><code>steps.&lt;name&gt;</code> only resolves the step's entire output \u2014 you can't chain a field path onto it directly (e.g. <code>steps.debug_a.variables.0.value</code> does not work). To reach into a field, nested object, or array inside a step's output, wrap the bare step reference in <code>jsonpath(...)</code> with the full path in the JSONPath string instead \u2014 see below.</p>"},{"location":"docs/expressions/#jsonpath","title":"JSONPath","text":"<p>For reaching into a field, nested object, or array inside a JSON value, use a JSONPath string starting with <code>$</code> as an argument to <code>jsonpath(...)</code>. Only basic paths are supported \u2014 no filter expressions like <code>[?(@.price&gt;10)]</code>.</p> <p>The source argument (first argument) should be a bare reference \u2014 the whole trigger payload or the whole output of a step \u2014 and the entire path, including any array indices, goes inside the JSONPath string. Don't chain a field path onto <code>steps.&lt;name&gt;</code> before passing it in.</p> <pre><code>jsonpath(trigger.payload, \"$.items[0].name\")\njsonpath(steps.http_call_a, \"$.response.data.id\")\njsonpath(steps.debug_a, \"$.variables[0].value\")\n</code></pre>"},{"location":"docs/expressions/#functions","title":"Functions","text":"<p>Functions can be nested inside one another and take other expressions as arguments.</p>"},{"location":"docs/expressions/#json","title":"JSON","text":"Function Signature Description <code>jsonpath</code> <code>jsonpath(source, \"$.path\")</code> Extracts a value from JSON using a JSONPath expression."},{"location":"docs/expressions/#date","title":"Date","text":"Function Signature Description <code>date.now</code> <code>date.now()</code> Returns the current datetime in ISO 8601 format. <code>date.add</code> <code>date.add(datetime, amount, \"unit\")</code> Shifts a datetime by an amount. Units: minutes, hours, days, months. <code>date.format</code> <code>date.format(datetime, \"YYYY-MM-DD\")</code> Formats a datetime using tokens like YYYY-MM-DD or DD.MM.YYYY HH:mm."},{"location":"docs/expressions/#string","title":"String","text":"Function Signature Description <code>str.concat</code> <code>str.concat(a, b)</code> Concatenates two or more strings. <code>str.trim</code> <code>str.trim(value)</code> Trims whitespace from both sides of a string. <code>str.lower</code> <code>str.lower(value)</code> Converts a string to lowercase. <code>str.upper</code> <code>str.upper(value)</code> Converts a string to uppercase."},{"location":"docs/expressions/#math","title":"Math","text":"Function Signature Description <code>math.add</code> <code>math.add(a, b)</code> Adds two numbers. <code>math.sub</code> <code>math.sub(a, b)</code> Subtracts b from a. <code>math.mul</code> <code>math.mul(a, b)</code> Multiplies two numbers. <code>math.div</code> <code>math.div(a, b)</code> Divides a by b."},{"location":"docs/expressions/#boolean-conditions","title":"Boolean &amp; conditions","text":"Function Signature Description <code>eq</code> <code>eq(a, b)</code> True if a equals b. <code>gt</code> <code>gt(a, b)</code> True if a is greater than b. <code>lt</code> <code>lt(a, b)</code> True if a is less than b. <code>and</code> <code>and(a, b)</code> Logical AND. <code>or</code> <code>or(a, b)</code> Logical OR. <code>not</code> <code>not(a)</code> Logical NOT."},{"location":"docs/expressions/#nesting-functions","title":"Nesting functions","text":"<p>Arguments can themselves be function calls, references, or literals, nested as deep as you need:</p> <pre><code>and(\n  eq(trigger.title, \"Product Updated\"),\n  gt(jsonpath(steps.http_call_a, \"$.response.status\"), 199)\n)\n\nstr.upper(str.trim(trigger.payload.title))\n\ndate.format(date.add(date.now(), 7, \"days\"), \"YYYY-MM-DD\")\n</code></pre>"},{"location":"docs/expressions/#editor-support","title":"Editor support","text":"<p>Expression fields use a Monaco-based editor with:</p> <ul> <li>Syntax highlighting for functions, <code>steps.*</code>, <code>secrets.*</code>, <code>vars.*</code>, context references, and JSONPath.</li> <li>Autocomplete for every function, the machine names of upstream steps, trigger payload fields (from the trigger's schema), and workspace secrets and variables.</li> <li>Inside a Loop step's downstream branch, <code>loop.item</code> and <code>loop.index</code> are also suggested.</li> </ul>"},{"location":"docs/storage/","title":"Storage","text":"<p>Store your own structured data inside Flixir \u2014 a product catalog, a list of VIP customers, running counters, anything with custom fields \u2014 and read, write, update, or delete it right from a workflow.</p>"},{"location":"docs/storage/#concepts","title":"Concepts","text":"<ul> <li>Storage \u2014 a named container with a schema you define, similar to a lightweight table. Created once, then reused by any workflow.</li> <li>Item \u2014 a single record inside a Storage, matching its schema. Each item gets an auto-generated id.</li> </ul>"},{"location":"docs/storage/#creating-a-storage","title":"Creating a Storage","text":"<p>Under Settings \u2192 Storage, create a Storage with a name and a list of fields. Each field has:</p> <ul> <li>a name</li> <li>a type: <code>string</code>, <code>number</code>, <code>boolean</code>, <code>date</code>/<code>datetime</code>, or <code>list</code></li> <li>whether it's required</li> </ul>"},{"location":"docs/storage/#schema-is-append-only","title":"Schema is append-only","text":"<p>Once a Storage is saved, existing fields can't be renamed, retyped, or removed \u2014 you can only add new fields. This keeps previously written items valid against the schema. Plan field names and types with this in mind, or create a new Storage if you need a substantially different structure.</p>"},{"location":"docs/storage/#using-storage-in-a-workflow","title":"Using Storage in a workflow","text":"<p>Four steps cover the full CRUD:</p> <ul> <li>Storage: Write \u2014 insert a new item.</li> <li>Storage: Read \u2014 look up items, with an optional single-field filter.</li> <li>Storage: Update \u2014 merge new values into an existing item, by id.</li> <li>Storage: Delete \u2014 permanently remove an item, by id.</li> </ul> <p>Items are identified by their auto-generated id \u2014 capture it from Storage: Write's output if you'll need to update or delete that item later.</p> <p>The Storage Changed trigger starts a workflow whenever an item in a chosen Storage is created, updated, or deleted.</p>"},{"location":"docs/storage/#limitations","title":"Limitations","text":"<ul> <li>Filtering in Storage: Read supports only a single field with an exact match \u2014 no multi-field or ranged filters yet.</li> <li>Deletes are permanent; there's no restore.</li> <li>Storage: Read returns at most 500 items per call (default 50).</li> <li>There's no relationship/reference between different Storages.</li> </ul> <p>Note</p> <p>Need a threshold like \"notify me once a Storage has over 100 items\"? Compose it yourself: Storage Changed trigger \u2192 Storage: Read \u2192 Condition step.</p>"},{"location":"docs/steps/condition/","title":"Condition step","text":"<p>step_type: condition</p> <p>Evaluates a boolean expression and splits the workflow into a true branch and a false branch, so downstream steps only run down the matching path.</p>"},{"location":"docs/steps/condition/#configuration","title":"Configuration","text":"Field Type Required Description <code>expression</code> string required A boolean expression evaluated against workflow data. Supports literals, <code>trigger.*</code> / <code>steps.*</code> / <code>env.*</code> references, <code>jsonpath(...)</code>, and nested functions like <code>math.add</code>, <code>str.upper</code>, <code>and</code>, <code>not</code>."},{"location":"docs/steps/condition/#example-expressions","title":"Example expressions","text":"<pre><code>eq(trigger.title, \"Product Updated\")\ngt(trigger.variants_count, 0)\neq(jsonpath(steps.condition_a, \"$.result\"), true)\nand(eq(1, 1), not(false))\n</code></pre>"},{"location":"docs/steps/condition/#when-to-use-it","title":"When to use it","text":"<ul> <li>Branching a workflow based on trigger data \u2014 e.g. only continue for products above a price threshold.</li> <li>Gating a step behind an environment or account flag.</li> <li>Combining outputs from earlier steps with boolean logic before acting on them.</li> </ul>"},{"location":"docs/steps/debug/","title":"Debug step","text":"<p>step_type: debug</p> <p>Writes the current state, or any variables you choose, to the workflow run log. It doesn't call anything external \u2014 it's a pure inspection tool.</p>"},{"location":"docs/steps/debug/#configuration","title":"Configuration","text":"Field Type Required Description <code>state</code> string required Free-text label for the returned step state, shown in the run log. <code>variables</code> array"},{"location":"docs/steps/debug/#when-to-use-it","title":"Debug step","text":""},{"location":"docs/steps/gemini/","title":"Gemini step","text":"<p>step_type: gemini</p> <p>Generates text with Google Gemini directly inside a workflow \u2014 write a prompt, optionally set a system instruction, and reference workflow data via expressions.</p>"},{"location":"docs/steps/gemini/#configuration","title":"Configuration","text":"Field Type Required Description <code>api_key</code> string optional Gemini API key. Use a workspace secret, e.g. <code>secrets.GEMINI_KEY</code>, rather than a literal key. <code>prompt</code> string required The user message sent to Gemini. Supports expressions \u2014 reference trigger or step output data. <code>system</code> string optional Optional system instruction to set the assistant's tone, role, or output format. <code>model</code> enum optional Gemini model to use: <code>gemini-2.0-flash</code> (default), <code>gemini-2.0-flash-lite</code>, <code>gemini-2.5-flash-preview-05-20</code>, or <code>gemini-2.5-pro-preview-06-05</code>. <code>temperature</code> number optional Controls randomness, from 0 (deterministic) to 2 (very creative). Default 1.0. <code>max_tokens</code> integer optional Maximum tokens in the generated response, from 1 to 8192. Default 1024."},{"location":"docs/steps/gemini/#when-to-use-it","title":"When to use it","text":"<ul> <li>Summarizing an order, product description, or support message mid-workflow.</li> <li>Classifying or tagging incoming data before branching with a Condition step.</li> <li>Drafting content \u2014 email copy, product descriptions \u2014 from structured trigger data.</li> </ul> <p>Note</p> <p><code>prompt</code> is expression-aware, so it can pull in live values like <code>trigger.payload.title</code>; <code>system</code> is treated as static text.</p>"},{"location":"docs/steps/http-request/","title":"HTTP Request step","text":"<p>step_type: http_call</p> <p>Calls any HTTP endpoint with a configurable method, query string, headers, and body. The response is made available to downstream steps.</p>"},{"location":"docs/steps/http-request/#configuration","title":"Configuration","text":"Field Type Required Description <code>method</code> enum required HTTP method: <code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>PATCH</code>, <code>DELETE</code>, or <code>HEAD</code>. Defaults to <code>GET</code>. <code>url</code> string (uri) required The request URL. <code>query</code> array"},{"location":"docs/steps/http-request/#when-to-use-it","title":"HTTP Request step","text":""},{"location":"docs/steps/loop/","title":"Loop step","text":"<p>step_type: loop</p> <p>Iterates over a list \u2014 products, order line items, or any array from trigger or step data \u2014 running its downstream branch once per item.</p>"},{"location":"docs/steps/loop/#configuration","title":"Configuration","text":"Field Type Required Description <code>list</code> string required Expression resolving to the list to iterate over. Each downstream branch runs once per item."},{"location":"docs/steps/loop/#when-to-use-it","title":"When to use it","text":"<ul> <li>Processing every line item in an order, or every variant of a product.</li> <li>Sending a notification or API call once per item in a list from a previous step.</li> <li>Batch operations that need the same set of steps repeated per record.</li> </ul>"},{"location":"docs/steps/lua-script/","title":"Lua script step","text":"<p>step_type: lua_script</p> <p>Runs a custom Lua script for logic that doesn't fit a built-in step \u2014 custom parsing, formatting, or calculations against workflow data.</p>"},{"location":"docs/steps/lua-script/#configuration","title":"Configuration","text":"Field Type Required Description <code>code</code> string (code, lua) required The Lua script to run, with access to the workflow's variables. <code>variables</code> array"},{"location":"docs/steps/lua-script/#when-to-use-it","title":"Lua script step","text":""},{"location":"docs/steps/shopify-graphql/","title":"Shopify GraphQL step","text":"<p>step_type: shopify_graphql</p> <p>Runs any Shopify Admin GraphQL query or mutation against the connected store, for reads or writes the built-in steps don't cover.</p>"},{"location":"docs/steps/shopify-graphql/#configuration","title":"Configuration","text":"Field Type Required Description <code>query</code> string (code, graphql) required The GraphQL query or mutation to execute against the Shopify Admin API. <code>variables</code> array"},{"location":"docs/steps/shopify-graphql/#when-to-use-it","title":"Shopify GraphQL step","text":""},{"location":"docs/steps/sleep/","title":"Sleep step","text":"<p>step_type: sleep</p> <p>Pauses the workflow for a set amount of time before continuing to the next step \u2014 without tying up any resources while it waits.</p>"},{"location":"docs/steps/sleep/#configuration","title":"Configuration","text":"Field Type Required Description <code>duration_value</code> number required How long to wait, in the unit selected below. Expression-aware. <code>duration_unit</code> string (select) required The unit for <code>duration_value</code>: <code>seconds</code>, <code>minutes</code>, <code>hours</code>, or <code>days</code>. Defaults to <code>minutes</code>. <p>The maximum wait is 30 days; a duration longer than that is rejected when you save the step.</p>"},{"location":"docs/steps/sleep/#when-to-use-it","title":"When to use it","text":"<ul> <li>Waiting a fixed amount of time before following up \u2014 e.g. checking in a day after an abandoned checkout.</li> <li>Spacing out a sequence of reminders or notifications.</li> <li>Holding fulfillment or a status change for a set number of hours before proceeding.</li> </ul> <p>Note</p> <p>While a workflow run is asleep, it isn't actively running \u2014 it resumes automatically once the wait is over, so there's no cost or resource use during the delay.</p>"},{"location":"docs/steps/storage-delete/","title":"Storage: Delete step","text":"<p>step_type: storage_delete</p> <p>Permanently deletes an item from a Storage by id.</p>"},{"location":"docs/steps/storage-delete/#configuration","title":"Configuration","text":"Field Type Required Description <code>storage</code> string required The Storage the item belongs to, identified by its machine name. <code>item_id</code> string required The id of the item to delete."},{"location":"docs/steps/storage-delete/#when-to-use-it","title":"When to use it","text":"<ul> <li>Cleaning up records that are no longer needed \u2014 expired entries, processed queue items, and so on.</li> <li>Removing a record as part of an undo/rollback path in a workflow.</li> </ul> <p>Warning</p> <p>Deletion is permanent \u2014 there's no soft delete or restore. Double-check <code>item_id</code> before wiring this step into a workflow, especially if it's driven by expression-based data.</p>"},{"location":"docs/steps/storage-read/","title":"Storage: Read step","text":"<p>step_type: storage_read</p> <p>Looks up items in a Storage, optionally filtered by a single field, and returns the matching items to the workflow.</p>"},{"location":"docs/steps/storage-read/#configuration","title":"Configuration","text":"Field Type Required Description <code>storage</code> string required The Storage to read from, identified by its machine name. <code>filter_field</code> string optional A single field to filter on. The available fields match the selected Storage's schema. Leave unset to return items without filtering. <code>filter_value</code> \u2014 optional The value <code>filter_field</code> must exactly match. Its type follows the selected field's type. <code>limit</code> integer optional Maximum number of items to return. Defaults to 50, hard capped at 500."},{"location":"docs/steps/storage-read/#output","title":"Output","text":"<p>Returns the matching <code>items</code> (each with its <code>id</code> and <code>data</code>) and a <code>count</code> of how many were returned.</p>"},{"location":"docs/steps/storage-read/#when-to-use-it","title":"When to use it","text":"<ul> <li>Looking up a record by a known field before acting on it \u2014 e.g. find the catalog item matching an incoming SKU.</li> <li>Pulling a list of items to loop over with a Loop step.</li> <li>Checking whether something exists before writing a new item, to avoid duplicates.</li> </ul> <p>Note</p> <p>Only one field can be filtered at a time, with an exact match \u2014 there's no support yet for multiple conditions or ranged/partial matches. <code>filter_value</code> is expression-aware, so it can come from trigger or step data.</p>"},{"location":"docs/steps/storage-update/","title":"Storage: Update step","text":"<p>step_type: storage_update</p> <p>Updates an existing item in a Storage by id. Only the fields you provide are changed \u2014 everything else on the item is left as-is.</p>"},{"location":"docs/steps/storage-update/#configuration","title":"Configuration","text":"Field Type Required Description <code>storage</code> string required The Storage the item belongs to, identified by its machine name. <code>item_id</code> string required The id of the item to update \u2014 e.g. from a previous Storage: Write or Storage: Read step. <code>values</code> object required The fields to change, merged into the existing item. Any fields left out keep their current value. Fields and types match the selected Storage's schema."},{"location":"docs/steps/storage-update/#output","title":"Output","text":"<p>The step's output includes the updated item's <code>id</code> and its full <code>data</code> after the merge.</p>"},{"location":"docs/steps/storage-update/#when-to-use-it","title":"When to use it","text":"<ul> <li>Updating a status or counter field on a record created earlier in a workflow (or a previous run).</li> <li>Applying a partial change \u2014 e.g. bumping a stock count \u2014 without needing to resend the whole item.</li> </ul> <p>Note</p> <p>This is a partial merge, not a full replace \u2014 you don't need to repeat unchanged fields in <code>values</code>. Values are expression-aware, same as Storage: Write.</p>"},{"location":"docs/steps/storage-write/","title":"Storage: Write step","text":"<p>step_type: storage_write</p> <p>Inserts a new item into a Storage. The values you provide are validated against that Storage's schema before the item is created.</p>"},{"location":"docs/steps/storage-write/#configuration","title":"Configuration","text":"Field Type Required Description <code>storage</code> string required The Storage to write into, identified by its machine name. <code>values</code> object required The item's field values. The available fields \u2014 and their types \u2014 match the selected Storage's schema, and are shown once <code>storage</code> is set."},{"location":"docs/steps/storage-write/#output","title":"Output","text":"<p>The step's output includes the new item's <code>id</code> and its saved <code>data</code>, so later steps (or a Storage: Update / Storage: Delete step) can reference it \u2014 e.g. <code>steps.storage_write_1.id</code>.</p>"},{"location":"docs/steps/storage-write/#when-to-use-it","title":"When to use it","text":"<ul> <li>Saving a custom record \u2014 an order note, a catalog entry, a log \u2014 as part of a workflow run.</li> <li>Building up your own dataset from incoming trigger data over time.</li> </ul> <p>Note</p> <p>Required fields must be present in <code>values</code>, or the step fails validation. Field values are expression-aware, so you can populate them from trigger or step data \u2014 e.g. <code>trigger.payload.title</code>. See the Expressions guide.</p>"},{"location":"docs/triggers/cron/","title":"Scheduled trigger","text":"<p>trigger_type: cron</p> <p>Runs a workflow on a recurring schedule, defined as a cron expression. No external event needed \u2014 Flixir fires the trigger itself at the configured times.</p>"},{"location":"docs/triggers/cron/#configuration","title":"Configuration","text":"Field Type Required Description <code>schedule</code> string required Standard 5-field cron expression (minute hour day month weekday). E.g. <code>0 9 * * 1</code> = every Monday at 9:00. <code>timezone</code> string optional IANA timezone the schedule is evaluated in, e.g. <code>Europe/Berlin</code>. Defaults to UTC."},{"location":"docs/triggers/cron/#presets","title":"Presets","text":"<p>Common schedules you can pick from directly, instead of writing a cron expression by hand:</p> Preset Description Cron expression Every hour Runs at the start of every hour <code>0 * * * *</code> Every day at 9:00 Runs daily at 9 AM UTC (great for morning reports) <code>0 9 * * *</code> Every Monday at 9:00 Weekly digest or report on Monday morning <code>0 9 * * 1</code> Every 15 minutes Frequent polling \u2014 inventory checks, sync jobs <code>*/15 * * * *</code> Every day at midnight Nightly cleanup or data archiving <code>0 0 * * *</code> 1st of every month Monthly report or billing cycle job <code>0 9 1 * *</code>"},{"location":"docs/triggers/cron/#when-to-use-it","title":"When to use it","text":"<ul> <li>Daily or weekly reports and digests.</li> <li>Periodic sync or cleanup jobs that don't depend on an external event.</li> <li>Polling an API on a fixed interval when no webhook is available.</li> </ul>"},{"location":"docs/triggers/manual/","title":"Manual Trigger","text":"<p>trigger_type: manual</p> <p>Starts a workflow run immediately when you click Run in the Flixir editor. There's no external event to configure \u2014 it's the fastest way to test a workflow while you're building it.</p>"},{"location":"docs/triggers/manual/#configuration","title":"Configuration","text":"<p>This trigger has no configurable fields. Attach it to a workflow and press run.</p>"},{"location":"docs/triggers/manual/#when-to-use-it","title":"When to use it","text":"<ul> <li>Testing a workflow while designing it, before wiring up a real trigger.</li> <li>One-off or ad-hoc runs that a teammate kicks off by hand.</li> <li>Workflows meant to be triggered from another workflow rather than an external event.</li> </ul> <p>Note</p> <p>because there's no external payload, downstream steps that expect trigger data (e.g. <code>trigger.title</code>) should provide sensible fallbacks when run manually.</p>"},{"location":"docs/triggers/shopify/","title":"Shopify trigger","text":"<p>trigger_type: shopify</p> <p>Starts a workflow whenever something happens in the connected Shopify store. Pick a specific event via a preset, and the trigger's payload schema is filled in automatically.</p>"},{"location":"docs/triggers/shopify/#configuration","title":"Configuration","text":"Field Type Required Description <code>filter</code> string optional Optional expression to filter which events actually start a run, evaluated against the incoming event payload."},{"location":"docs/triggers/shopify/#event-presets","title":"Event presets","text":"<p>Each preset maps to a Shopify webhook topic and comes with its own example payload and schema, used to validate expressions that read from <code>trigger.payload</code>:</p> Preset Description Webhook topic New Product Fires when a new product is created in the connected store. <code>products/create</code> Product Updated Fires when an existing product is updated \u2014 title, price, variants, and more. <code>products/update</code> Inventory Updated Fires when stock levels change for a product variant at a location. <code>inventory_levels/update</code>"},{"location":"docs/triggers/shopify/#when-to-use-it","title":"When to use it","text":"<ul> <li>Reacting to catalog changes \u2014 new products, edits, price changes.</li> <li>Keeping inventory in sync with an external system.</li> <li>Any automation that should start from something happening in the merchant's store, rather than on a schedule or external webhook.</li> </ul> <p>Note</p> <p><code>has_filter</code> is enabled for this trigger, so you can narrow down which events reach the workflow before any steps run.</p>"},{"location":"docs/triggers/storage-changed/","title":"Storage Changed trigger","text":"<p>trigger_type: storage_changed</p> <p>Starts a workflow whenever an item in a chosen Storage is created, updated, or deleted \u2014 whether that change came from a Storage: Write/Update/Delete step in another workflow, or the same one.</p>"},{"location":"docs/triggers/storage-changed/#configuration","title":"Configuration","text":"Field Type Required Description <code>storage</code> string required The Storage to watch, identified by its machine name. <code>action_filter</code> enum optional Restrict to one kind of change: <code>created</code>, <code>updated</code>, or <code>deleted</code>. Leave unset to fire on all three."},{"location":"docs/triggers/storage-changed/#payload","title":"Payload","text":"<p>The triggered run's <code>trigger.payload</code> includes:</p> Field Description <code>storage_machine_name</code> The Storage the change happened in. <code>action</code> Which change fired the run: <code>created</code>, <code>updated</code>, or <code>deleted</code>. <code>item_id</code> The id of the affected item. <code>data</code> The item's field values at the time of the change."},{"location":"docs/triggers/storage-changed/#when-to-use-it","title":"When to use it","text":"<ul> <li>Reacting whenever a record is added to a catalog or list you maintain in Storage.</li> <li>Chaining workflows together \u2014 one workflow writes to Storage, another reacts to that change.</li> <li>Keeping an external system in sync with data you store in Flixir.</li> </ul> <p>Note</p> <p>There's no built-in threshold trigger (e.g. \"fire once a Storage passes 100 items\") \u2014 each change fires the trigger once. Build thresholds yourself with Storage Changed \u2192 Storage: Read \u2192 Condition.</p>"},{"location":"docs/triggers/webhook/","title":"Webhook","text":"<p>trigger_type: webhook</p> <p>Gives every workflow a unique HTTP endpoint. Sending a request to that URL starts a run, with the request body available as trigger data to every downstream step.</p>"},{"location":"docs/triggers/webhook/#configuration","title":"Configuration","text":"<p>No fields to fill in \u2014 the webhook URL is generated automatically once the trigger is attached to a workflow.</p>"},{"location":"docs/triggers/webhook/#when-to-use-it","title":"When to use it","text":"<ul> <li>Receiving events from a third-party system that isn't Shopify.</li> <li>Connecting a custom app, form, or internal tool to a Flixir workflow.</li> <li>Chaining workflows together by calling one workflow's webhook from another.</li> </ul> <p>Note</p> <p>the incoming request body is exposed to later steps and conditions as <code>trigger.payload</code>.</p>"}]}